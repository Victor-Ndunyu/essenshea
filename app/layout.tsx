export const metadata = {
  title: 'Essenshea — Natural Luxury Beauty',
  description: 'Premium natural beauty boutique offering body butters, carrier oils, essential oils, hydrosols, gift sets, haircare, and raw butters.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
